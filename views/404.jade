extends layout

block includes
  != css('404')

block content
  h1 Dude, <code>#{url}</code> doesnt exist!
  if laws
    a(href='/app').fill.round Take me to the homepage!
  else
    a(href='/').fill.round Take me to the homepage!
